const layer=document.getElementById("markers"),info=document.getElementById("info");let filter="all";function render(){layer.innerHTML="";EVENTS.forEach(e=>{if(filter!=="all"&&!e.types.includes(filter))return;const g=document.createElementNS("http://www.w3.org/2000/svg","g");g.setAttribute("class","marker "+e.kind);g.innerHTML=`<circle class="outer" cx="${e.x}" cy="${e.y}" r="14"/><circle class="inner" cx="${e.x}" cy="${e.y}" r="9"/><text class="label" x="${e.x+19}" y="${e.y+5}">${e.name}</text>`;g.onclick=()=>info.innerHTML=`<b>${e.name}</b><div class="meta">31.08.2026</div><p>${e.text}</p><div class="meta"><b>Источник:</b> ${e.source}</div>`;layer.appendChild(g)})}document.querySelectorAll("#filters button").forEach(b=>b.onclick=()=>{document.querySelectorAll("#filters button").forEach(x=>x.classList.remove("active"));b.classList.add("active");filter=b.dataset.filter;render()});render();